# Private Knowledge Documents

This directory is the local source for private documents indexed by Echo. Text documents are intentionally ignored by Git because they may contain member names, contact details, meeting notes, or internal policies.

Place the deployment's source `.txt` files here before running the document sync. Do not commit real choir documents to a public repository.
